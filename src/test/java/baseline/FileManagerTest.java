package baseline;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class FileManagerTest {

    @Test
    void getListFormatted() {
        // create a list
        // assert that the formatted string from the list is the same as our expected one
    }
}